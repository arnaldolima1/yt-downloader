import { HomeIcon, ImageIcon, SettingsIcon } from "lucide-react-native";
import { iconWithClassName } from "@/lib/iconWithClassName";
iconWithClassName(HomeIcon);
iconWithClassName(ImageIcon);
iconWithClassName(SettingsIcon);

export { HomeIcon, ImageIcon, SettingsIcon };
